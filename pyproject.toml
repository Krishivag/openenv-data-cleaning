[project]
name = "data_cleaning_env"
version = "0.1.0"
description = "Data cleaning environment"
dependencies = [
    "openenv-core",
    "pydantic",
    "openai",
    "fastapi",
    "uvicorn",
    "requests",
    "httpx"
]

[project.scripts]
server = "server.app:main"
